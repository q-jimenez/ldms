load name=${plugname}
config name=${plugname} producer=localhost${i} schema=${plugname} instance=localhost${i}/${plugname} component_id=${i} conf=${PERFCONF}
start name=${plugname} interval=1000000 offset=0
